name: Validate Guides Manifest

on:
  pull_request:
    branches: [main]
    paths:
      - 'guides.json'
      - '*.html'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate guides.json
        run: python3 .github/scripts/validate.py
